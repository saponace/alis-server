  # Wekan database
  wekan_db:
    image: mongo:%VERSION%
    container_name: wekan_db
    restart: always
    command: mongod --smallfiles --oplogSize 128
    networks:
      - wekan_net
    expose:
      - 27017
    volumes:
      - %DATA_DIR%/db:/var/lib/mysql
      - %DATA_DIR%/dump:/dump
