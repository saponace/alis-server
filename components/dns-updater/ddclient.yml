  # DNS entry updater
  ddclient:
    container_name: ddclient
    image: linuxserver/ddclient:3.9.0-ls12
    restart: unless-stopped
    environment:
      - PUID=%DOCKER_USER_ID%
      - PGID=%DOCKER_GROUP_ID%
      - TZ=%TIMEZONE%
    volumes:
      - %CONTAINERS_DATA_DIR%/ddclient:/config
