  # Movies PVR
  radarr:
    container_name: radarr
    image: linuxserver/radarr:2.0.0.2051-ls23
    restart: unless-stopped
    network_mode: service:pia_client
    depends_on:
      - jackett
      - transmission
      - notifications
    volumes:
      - %CONTAINERS_CONFIG_DIR%/radarr:/config
      - %TORRENTS_DOWNLOADS_DIR%:/downloads
      - %MOVIES_DIR%:/movies
      - /etc/localtime:/etc/localtime:ro
      - %DOCKER_SHARED_DIR%:/shared
    environment:
      - PUID=%DOCKER_USER_ID%
      - PGID=%DOCKER_GROUP_ID%
      - TZ=%TIMEZONE%
    labels:
      - traefik.http.routers.radarr.middlewares=auth@file
      - traefik.http.services.radarr.loadbalancer.server.port=7878
