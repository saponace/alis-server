  # Filebrowser
  filebrowser:
    container_name: filebrowser
    image: filebrowser/filebrowser:v2.0.12
    restart: unless-stopped
    volumes:
      - /mnt:/srv
      - %CONTAINERS_DATA_DIR%/filebrowser/database.db:/database.db
    labels:
      - traefik.enable=true
      - traefik.http.routers.filebrowser.rule=Host(`filebrowser.%DOMAIN_NAME%`)
      - traefik.http.services.filebrowser.loadbalancer.server.port=80
      - traefik.http.routers.filebrowser.tls.certresolver=le
